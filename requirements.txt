# Web Framework & Interface
streamlit>=1.35.0
plotly>=5.20.0

# Core AI Engine
google-genai>=2.22.0,<3.0.0

# ClickHouse
clickhouse-connect>=0.7.0

# Computer Vision
# Headless build is required for Streamlit Cloud / Linux servers.
opencv-python-headless==4.11.0.86

# Audio Analysis
librosa>=0.10.1

# Data & Environment
# Pin NumPy 1.x for stable OpenCV/scientific-stack compatibility.
numpy==1.26.4
pandas>=2.2.0
python-dotenv>=1.0.1
pydantic>=2.7.0

# HTTP / Reverse Search
requests>=2.31.0
urllib3>=2.2.0

# Images
Pillow>=10.3.0

# Public video-page URL resolver
# default => yt-dlp-ejs and its recommended Python-side dependencies
# curl-cffi => browser/TLS impersonation support, useful on hosted servers
yt-dlp[default,curl-cffi]>=2026.8.19

# Optional/advanced YouTube PO-token provider plugin.
# To use it in production, run the matching private bgutil provider service
# and set YTDLP_POT_PROVIDER_URL in Streamlit Secrets/environment.
bgutil-ytdlp-pot-provider>=2.0.0

# Bundled FFmpeg fallback
imageio-ffmpeg>=0.5.1

# PDF Reports
reportlab>=4.2.0

# Cloudflare R2 / S3-compatible storage
boto3>=1.34.0
